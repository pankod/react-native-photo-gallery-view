import { ImageStyle, ViewStyle } from "react-native";
export declare const PreviewModalStyle: {
    container: ViewStyle;
    imagePreview: ViewStyle;
    thumbPreview: ImageStyle;
    containPreview: ImageStyle;
};
